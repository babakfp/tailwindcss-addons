---
title: Background Radial
---

import { Code } from "@astrojs/starlight/components"
import dedent from "dedent"
import ApiTable from "../../../components/ApiTable.svelte"
import Preview from "../../../components/Preview.svelte"

<ApiTable
    rows={[
        [
            "bg-radial-<theme-color>",
            dedent`
                background-size: var(--tw-bg-radial-gap-x, 32px) var(--tw-bg-radial-gap-y, 32px);
                background-image: radial-gradient(circle, <theme-color> var(--tw-bg-radial-size, 1px), transparent 0);
            `,
        ],
        [
            "bg-radial-(<custom-property>)",
            dedent`
                background-size: var(--tw-bg-radial-gap-x, 32px) var(--tw-bg-radial-gap-y, 32px);
                background-image: radial-gradient(circle, var(<custom-property>) var(--tw-bg-radial-size, 1px), transparent 0);
            `,
        ],
        [
            "bg-radial-[<arbitrary-value>]",
            dedent`
                background-size: var(--tw-bg-radial-gap-x, 32px) var(--tw-bg-radial-gap-y, 32px);
                background-image: radial-gradient(circle, <arbitrary-value> var(--tw-bg-radial-size, 1px), transparent 0);
            `,
        ],
    ]}
/>

## Examples

export const examples = {
    _base_: "h-48",
    basic: "bg-radial-gray-700",
    center: "bg-center",
    shadow: "shadow-[inset_0_0_2rem_2rem_var(--sl-color-black)]",
    gap: "bg-radial-gap-4",
    gapX: "bg-radial-gap-x-16",
    gapY: "bg-radial-gap-y-16",
    size: "bg-radial-size-2",
}

### Basic

<Preview useGrid={false}>
    <div class={`${examples.basic} ${examples._base_}`} />
</Preview>

<Code
    code={`<div class="${examples.basic} ${examples._base_}"></div>`}
    lang="jsx"
    mark={[examples.basic]}
/>

### Center

<Preview useGrid={false}>
    <div class={`${examples.center} ${examples._base_} ${examples.basic}`} />
</Preview>

<Code
    code={`<div class="${examples.center} ${examples._base_} ${examples.basic}"></div>`}
    lang="jsx"
    mark={[examples.center]}
/>

### Shadow

<Preview useGrid={false}>
    <div
        class={`${examples.shadow} ${examples._base_} ${examples.basic} ${examples.center}`}
    />
</Preview>

<Code
    code={`<div class="${examples.shadow} ${examples._base_} ${examples.basic} ${examples.center}"></div>`}
    lang="jsx"
    mark={[examples.shadow]}
/>

### Spacing

#### X and Y

<ApiTable
    rows={[
        [
            "bg-radial-gap-<theme-spacing>",
            dedent`
                --tw-bg-radial-gap-x: calc(var(--spacing) * <theme-spacing>);
                --tw-bg-radial-gap-y: calc(var(--spacing) * <theme-spacing>);
            `,
        ],
        [
            "bg-radial-gap-<number>",
            dedent`
                --tw-bg-radial-gap-x: calc(var(--spacing) * <number>);
                --tw-bg-radial-gap-y: calc(var(--spacing) * <number>);
            `,
        ],
        [
            "bg-radial-gap-(<custom-property>)",
            dedent`
                --tw-bg-radial-gap-x: var(<custom-property>);
                --tw-bg-radial-gap-y: var(<custom-property>);
            `,
        ],
        [
            "bg-radial-gap-[<arbitrary-value>]",
            dedent`
                --tw-bg-radial-gap-x: <arbitrary-value>;
                --tw-bg-radial-gap-y: <arbitrary-value>;
            `,
        ],
    ]}
/>

<Preview useGrid={false}>
    <div
        class={`${examples.gap} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}`}
    />
</Preview>

<Code
    code={`<div class="${examples.gap} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}"></div>`}
    lang="jsx"
    mark={[examples.gap]}
/>

#### X

<ApiTable
    rows={[
        [
            "bg-radial-gap-x-<theme-spacing>",
            dedent`
                --tw-bg-radial-gap-x: calc(var(--spacing) * <theme-spacing>);
            `,
        ],
        [
            "bg-radial-gap-x-<number>",
            dedent`
                --tw-bg-radial-gap-x: calc(var(--spacing) * <number>);
            `,
        ],
        [
            "bg-radial-gap-x-(<custom-property>)",
            dedent`
                --tw-bg-radial-gap-x: var(<custom-property>);
            `,
        ],
        [
            "bg-radial-gap-x-[<arbitrary-value>]",
            dedent`
                --tw-bg-radial-gap-x: <arbitrary-value>;
            `,
        ],
    ]}
/>

<Preview useGrid={false}>
    <div
        class={`${examples.gapX} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}`}
    />
</Preview>

<Code
    code={`<div class="${examples.gapX} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}"></div>`}
    lang="jsx"
    mark={[examples.gapX]}
/>

#### Y

<ApiTable
    rows={[
        [
            "bg-radial-gap-y-<theme-spacing>",
            dedent`
                --tw-bg-radial-gap-y: calc(var(--spacing) * <theme-spacing>);
            `,
        ],
        [
            "bg-radial-gap-y-<number>",
            dedent`
                --tw-bg-radial-gap-y: calc(var(--spacing) * <number>);
            `,
        ],
        [
            "bg-radial-gap-y-(<custom-property>)",
            dedent`
                --tw-bg-radial-gap-y: var(<custom-property>);
            `,
        ],
        [
            "bg-radial-gap-y-[<arbitrary-value>]",
            dedent`
                --tw-bg-radial-gap-y: <arbitrary-value>;
            `,
        ],
    ]}
/>

<Preview useGrid={false}>
    <div
        class={`${examples.gapY} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}`}
    />
</Preview>

<Code
    code={`<div class="${examples.gapY} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}"></div>`}
    lang="jsx"
    mark={[examples.gapY]}
/>

## Size

<ApiTable
    rows={[
        ["bg-radial-size-<number>", "--tw-bg-radial-size: <number>px;"],
        [
            "bg-radial-size-(length:<custom-property>)",
            "--tw-bg-radial-size: var(<custom-property>);",
        ],
        [
            "bg-radial-size-[<arbitrary-value>]",
            "--tw-bg-radial-size: <arbitrary-value>;",
        ],
    ]}
/>

<Preview useGrid={false}>
    <div
        class={`${examples.size} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}`}
    />
</Preview>

<Code
    code={`<div class="${examples.size} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}"></div>`}
    lang="jsx"
    mark={[examples.size]}
/>

## How to import?

[How to import?](/#how-to-import)

## Resources

- [Hero Patterns](https://heropatterns.com)
    - [NPM: `tailwind-heropatterns`](https://www.npmjs.com/package/tailwind-heropatterns)
    - [NPM: `tailwindcss-hero-patterns`](https://www.npmjs.com/package/tailwindcss-hero-patterns)
