---
title: Background Grid
---

import { Code } from "@astrojs/starlight/components"
import dedent from "dedent"
import ApiTable from "../../../components/ApiTable.astro"
import Preview from "../../../components/Preview.astro"

<ApiTable
    rows={[
        [
            "bg-grid-<theme-color>",
            dedent`
                background-size: var(--tw-bg-grid-w, 32px) var(--tw-bg-grid-h, 32px);
                background-image: linear-gradient(to right, var(--<theme-color>) var(--tw-bg-grid-border-w, 1px), transparent 0px), linear-gradient( to bottom, var(--<theme-color>) var(--tw-bg-grid-border-w, 1px), transparent 0px);
            `,
        ],
        [
            "bg-grid-(<custom-property>)",
            dedent`
                background-size: var(--tw-bg-grid-w, 32px) var(--tw-bg-grid-h, 32px);
                background-image: linear-gradient(to right, var(<custom-property>) var(--tw-bg-grid-border-w, 1px), transparent 0px), linear-gradient( to bottom, var(<custom-property>) var(--tw-bg-grid-border-w, 1px), transparent 0px);
            `,
        ],
        [
            "bg-grid-[<arbitrary-value>]",
            dedent`
                background-size: var(--tw-bg-grid-w, 32px) var(--tw-bg-grid-h, 32px);
                background-image: linear-gradient(to right, <arbitrary-value> var(--tw-bg-grid-border-w, 1px), transparent 0px), linear-gradient( to bottom, <arbitrary-value> var(--tw-bg-grid-border-w, 1px), transparent 0px);
            `,
        ],
    ]}
/>

## Examples

export const examples = {
    _base_: "h-32",
    basic: "bg-grid-gray-800",
    center: "bg-center",
    shadow: "shadow-[inset_0_0_4rem_2rem_var(--sl-color-black)]",
    size: "bg-grid-size-4",
    width: "bg-grid-w-16",
    height: "bg-grid-h-16",
    borderWidth: "bg-grid-border-2",
}

### Basic

<Preview useGrid={false}>
    <div class={`${examples.basic} ${examples._base_}`} />
</Preview>

<Code
    code={`<div class="${examples.basic} ${examples._base_}"></div>`}
    lang="jsx"
    mark={[examples.basic]}
/>

### Center

<Preview useGrid={false}>
    <div class={`${examples.center} ${examples._base_} ${examples.basic}`} />
</Preview>

<Code
    code={`<div class="${examples.center} ${examples._base_} ${examples.basic}"></div>`}
    lang="jsx"
    mark={[examples.center]}
/>

### Shadow

<Preview useGrid={false}>
    <div
        class={`${examples.shadow} ${examples._base_} ${examples.basic} ${examples.center}`}
    />
</Preview>

<Code
    code={`<div class="${examples.shadow} ${examples._base_} ${examples.basic} ${examples.center}"></div>`}
    lang="jsx"
    mark={[examples.shadow]}
/>

### Size

<ApiTable
    rows={[
        [
            "bg-grid-size-<theme-spacing>",
            dedent`
                --tw-bg-grid-w: calc(var(--spacing) * <theme-spacing>);
                --tw-bg-grid-h: calc(var(--spacing) * <theme-spacing>);
            `,
        ],
        [
            "bg-grid-size-<number>",
            dedent`
                --tw-bg-grid-w: calc(var(--spacing) * <number>);
                --tw-bg-grid-h: calc(var(--spacing) * <number>);
            `,
        ],
        [
            "bg-grid-size-(<custom-property>)",
            dedent`
                --tw-bg-grid-w: var(<custom-property>);
                --tw-bg-grid-h: var(<custom-property>);
            `,
        ],
        [
            "bg-grid-size-[<arbitrary-value>]",
            dedent`
                --tw-bg-grid-w: <arbitrary-value>;
                --tw-bg-grid-h: <arbitrary-value>;
            `,
        ],
    ]}
/>

<Preview useGrid={false}>
    <div
        class={`${examples.size} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}`}
    />
</Preview>

<Code
    code={`<div class="${examples.size} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}"></div>`}
    lang="jsx"
    mark={[examples.size]}
/>

### Width

<ApiTable
    rows={[
        [
            "bg-grid-w-<theme-spacing>",
            "--tw-bg-grid-w: calc(var(--spacing) * <theme-spacing>);",
        ],
        [
            "bg-grid-w-<number>",
            "--tw-bg-grid-w: calc(var(--spacing) * <number>);",
        ],
        [
            "bg-grid-w-(<custom-property>)",
            "--tw-bg-grid-w: var(<custom-property>);",
        ],
        ["bg-grid-w-[<arbitrary-value>]", "--tw-bg-grid-w: <arbitrary-value>;"],
    ]}
/>

<Preview useGrid={false}>
    <div
        class={`${examples.width} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}`}
    />
</Preview>

<Code
    code={`<div class="${examples.width} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}"></div>`}
    lang="jsx"
    mark={[examples.width]}
/>

### Height

<ApiTable
    rows={[
        [
            "bg-grid-h-<theme-spacing>",
            "--tw-bg-grid-h: calc(var(--spacing) * <theme-spacing>);",
        ],
        [
            "bg-grid-h-<number>",
            "--tw-bg-grid-h: calc(var(--spacing) * <number>);",
        ],
        [
            "bg-grid-h-(<custom-property>)",
            "--tw-bg-grid-h: var(<custom-property>);",
        ],
        ["bg-grid-h-[<arbitrary-value>]", "--tw-bg-grid-h: <arbitrary-value>;"],
    ]}
/>

<Preview useGrid={false}>
    <div
        class={`${examples.height} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}`}
    />
</Preview>

<Code
    code={`<div class="${examples.height} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}"></div>`}
    lang="jsx"
    mark={[examples.height]}
/>

### Border Width

<ApiTable
    rows={[
        ["bg-grid-border-<number>", "--tw-bg-grid-border-w: <number>px;"],
        [
            "bg-grid-border-(length:<custom-property>)",
            "--tw-bg-grid-border-w: var(<custom-property>);",
        ],
        [
            "bg-grid-border-[<arbitrary-value>]",
            "--tw-bg-grid-border-w: <arbitrary-value>;",
        ],
    ]}
/>

<Preview useGrid={false}>
    <div
        class={`${examples.borderWidth} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}`}
    />
</Preview>

<Code
    code={`<div class="${examples.borderWidth} ${examples._base_} ${examples.basic} ${examples.center} ${examples.shadow}"></div>`}
    lang="jsx"
    mark={[examples.borderWidth]}
/>

## How to import?

[How to import?](/#how-to-import)

## Resources

- [Hero Patterns](https://heropatterns.com)
    - [NPM: `tailwind-heropatterns`](https://www.npmjs.com/package/tailwind-heropatterns)
    - [NPM: `tailwindcss-hero-patterns`](https://www.npmjs.com/package/tailwindcss-hero-patterns)
